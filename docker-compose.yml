services:
  discord-bot:
    image: ghcr.io/11gather11/discord-bot:latest@sha256:c44fcb95cd854596e32919863f5d8121cd121237221bfe82f37d5fae23eee6a6
    container_name: discord-bot
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - discord-bot-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  discord-bot-network:
    driver: bridge
